
// /* Write a function, getHardestHomework that takes in an array of homework objects and returns the name of the hardest homework assignment. The hardest homework will be the one that has the lowest average score. If the array contains no homeworks, return an empty string.
// */


// let sum = 0;
// scores = Object.values(homework)  

function getHardestHomework(grades){
  
// for (let val in homework){
//   sum += homework[val];
}
getHardestHomework([{ name: 'hip-hip-array', averageScore: 12 }, { name: 'wait-i-object', averageScore: 200 }, { name: 'objective-reasoning', averageScore: 25 }]);

console.log(getHardestHomework(grades));

  //add score together to get total

  // divide total by nuber of scores
  // if array is empty return an empty string -- 
  // if(statement)
  // return totalScore;

// }
// g
// console.log(totalScore);


// function getPlaylistLength(playlist){
// let newPlaylist = playlist.songs.length;
// return newPlaylist;
// }
// getPlaylistLength(music1)
// console.log(music1.songs.length);

// for (let i = 0; i < grades.length; i++) {
//   sum += homework1[i];
// }if(grades === 0){
//   return [];
// }